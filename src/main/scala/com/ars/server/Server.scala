package com.ars.server

object Server extends App {
  // TODO
  println(
    """ARS is up
      |POST api/model/store    to load models
      |POST api/model/validate to a validate a request data against its corresponding model"
      |""".stripMargin)
}
